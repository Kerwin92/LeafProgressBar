package com.kerwin.leafProgressBar;

import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;

import com.cpxiao.leafprogressbar.R;
import com.kerwin.leafProgressBar.widget.LeafProgressBar;

public class MainActivity extends AppCompatActivity {
    private LeafProgressBar mLeafProgressBar;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.leaf_loading_layout);
        //        mLeafProgressBar = (LeafProgressBar) findViewById(R.id.leaf_loading);
        //        mLeafProgressBar.set
    }
}
